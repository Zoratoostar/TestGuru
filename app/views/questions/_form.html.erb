
<%= form_with model: [@test, @question], local: true do |form| %>
  <%= render 'shared/errors', resource: @question %>

  <p>
    <%= form.label :body, "Text" %><br>
    <%= form.text_area :body, id: :question_body, cols: 50, rows: 10 %>
  </p>

  <p><%= form.submit %></p>
<% end %>
